# NEXUS-AIKALABS — Redis Configuration
# Optimized for 16GB VPS with multiple consumers (Directus cache, BullMQ)

maxmemory 256mb
maxmemory-policy allkeys-lru
save 900 1
save 300 10
save 60 10000
appendonly yes
appendfsync everysec
